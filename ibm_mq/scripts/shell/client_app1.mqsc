DEFINE QLOCAL(APP1.SAMPLE1.QUEUE1);
DEFINE CHANNEL(SQM1.APP1.SVRCONN) CHLTYPE(SVRCONN) TRPTYPE(TCP);
DEFINE LISTENER(SQM1LSNR) TRPTYPE(TCP) CONTROL(QMGR) PORT(1414);
*
SET AUTHREC OBJTYPE(QMGR) GROUP('mqapp') AUTHADD(CONNECT);
SET AUTHREC PROFILE(APP1.**) OBJTYPE(QUEUE) GROUP('mqapp') AUTHADD(PUT,GET);
SET CHLAUTH(SQM1.APP1.SVRCONN) TYPE(ADDRESSMAP) ADDRESS('255.255.255.255') MCAUSER('mqapp');
*
START LISTENER(SQM1LSNR);
