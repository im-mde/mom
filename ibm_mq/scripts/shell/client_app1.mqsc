DEFINE QLOCAL(MQAPP.SAMPLE.TEST);
DEFINE CHANNEL(TRN1QM.APP1.SVRCONN) CHLTYPE(SVRCONN) TRPTYPE(TCP);
DEFINE LISTENER(LISTENER1) TRPTYPE(TCP) CONTROL(QMGR) PORT(1414);
*
SET AUTHREC OBJTYPE(QMGR) GROUP('mqapp') AUTHADD(CONNECT);
SET AUTHREC PROFILE(MQAPP.**) OBJTYPE(QUEUE) GROUP('mqapp') AUTHADD(PUT,GET);
SET CHLAUTH(TRN1QM.APP1.SVRCONN) TYPE(ADDRESSMAP) ADDRESS('255.255.255.255') MCAUSER('mqapp');
*
START LISTENER(LISTENER1);
